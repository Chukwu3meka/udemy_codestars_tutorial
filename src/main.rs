fn main() {
    // ? Array
    // * Arrays are like tuples just that they hold the same value

    let mut arr = [1, 2, 3];

    println!("{}", arr[0]);

    arr[1] = 10;

    arr[10] = 200;

    println!("{}", arr[10])
}
